apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: ecommerce-dev
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8700
    targetPort: 8700
    protocol: TCP
    name: http
  selector:
    app: user-service

