apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: ecommerce-dev
  labels:
    app: shipping-service
spec:
  type: ClusterIP
  ports:
  - port: 8600
    targetPort: 8600
    protocol: TCP
    name: http
  selector:
    app: shipping-service

