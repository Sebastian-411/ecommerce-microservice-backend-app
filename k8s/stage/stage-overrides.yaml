apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: ecommerce-stage
spec:
  template:
    spec:
      containers:
      - name: "*"
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "stage"
        imagePullPolicy: IfNotPresent

